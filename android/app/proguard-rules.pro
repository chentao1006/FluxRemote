# App-specific R8 keep rules belong here.
#
# Keep this file intentionally narrow: libraries contribute their own consumer
# rules, and additional entries should be added only when release testing shows
# that code reached dynamically (for example, through reflection) was removed.
